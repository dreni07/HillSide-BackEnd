import { AiConfigStudioContainer } from '../components/studio';

/**
 * AI configuration — Agent Orchestration Studio (route: /app/ai-config).
 */
export function AiConfigPage() {
  return (
    <div className="ai-config-studio-root">
      <AiConfigStudioContainer />
    </div>
  );
}
